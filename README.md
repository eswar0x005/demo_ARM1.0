# demo_ARM1.0

Throwaway Salesforce metadata repository used as the version-control source for
ARM 1.0 (rabit) CI job integration tests.

Metadata API format at the repository root — the layout a `VC2SF` CI job expects when the
job's metadata folder path is left blank.
