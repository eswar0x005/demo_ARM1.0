# demo_ARM1.0

Throwaway Salesforce metadata repository used as the version-control source for
ARM 1.0 (rabit) CI job integration tests.

`src/` is classic Metadata API format — the layout a `VC2SF` CI job expects.
